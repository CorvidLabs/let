# let

Follow `AGENTS.md` and `docs/design.md`.

CorvidLabs web conventions: use **Bun** (`bun test`, `bun install`). Prefer `fledge run test` when fledge is available.

Hero product surface (when implemented):

```bash
let find worktrees --json
let where .
let context
let doctor --json
```
